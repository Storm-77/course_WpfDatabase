<UserControl x:Class="DbProject.views.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DbProject.views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock >Login:</TextBlock>
            <TextBox Margin="0 0 0 10" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock >Password:</TextBlock>
            <PasswordBox Margin="0 0 0 20" PasswordChanged="PasswordChanged"/>
            <Button Height="30px" Command="{Binding LoginCommand}">Login</Button>

        </StackPanel>
    </Grid>
</UserControl>
